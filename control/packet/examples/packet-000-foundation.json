{
  "packet_id": "packet-000-foundation",
  "title": "Repo foundation + namespaces",
  "domain": "engineering",
  "area": "engineering",
  "status": "DRAFT",
  "purpose": "Establish base repo layout, packet locations, and Codex skill skeletons without implementing runtime tools yet.",
  "scope_boundary": [
    "No runtime tool implementation (tools/* are placeholders only)",
    "No CI wiring in this packet",
    "No policy/OPA integration"
  ],
  "inputs": {
    "required_paths": [],
    "notes": "Base packet to establish the directory skeleton."
  },
  "outputs": [
    ".codex/packet/README.md",
    ".codex/skills/packet-runner/SKILL.md",
    "control/packet/packet_contract.schema.json"
  ],
  "end_state_predicate": {
    "commands": [
      "python -c \"import json; json.load(open('control/packet/packet_contract.schema.json'))\""
    ],
    "expected": [
      "Schema file is valid JSON and parses without error.",
      "Directory skeleton exists."
    ]
  },
  "evidence_artifacts": [
    "control/packet/examples/packet-000-foundation.json"
  ],
  "failure_modes": [
    "Schema JSON invalid",
    "Missing required directories/files"
  ],
  "forbidden_actions": [
    "Editing outside the base skeleton paths"
  ]
}